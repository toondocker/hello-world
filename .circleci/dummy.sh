#!/usr/bin/with-contenv sh
# shellcheck shell=sh
# dummy script so that I can use 'standard' CircleCI config.yml
# which requires there to be at least one script file due to
# the use of ShellCheck
exit 0